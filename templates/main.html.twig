{% extends 'base.html.twig' %}
    {% block body %}
        <main>
            <div>
                <h1>Liste des articles</h1>
                {% for article in articles %}
                    <h2>{{ article.name }}</h2>
                    <p>{{ article.text }}</p>
                {% endfor %}
            </div>
        </main>
    {% endblock %}
